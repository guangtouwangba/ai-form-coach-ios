# 云端模型只复盘已经结束的训练组

产品采用分层推理：基础实时分析始终在设备端完成，并负责姿态观察、阶段识别、计数、高置信度问题判定和实时提示；高级档可以在训练组结束后异步调用 Gemini 等云端多模态模型，综合端侧结构化证据和关键帧，生成更深入的解释、问题排序和行动建议。云端模型不进入实时训练链路，也不成为计数或动作问题的唯一事实来源。发送短视频必须另获用户明确同意；网络不可用、云端失败或用户拒绝上传时，端侧训练闭环和组总结仍然可用。该边界以可预测延迟、成本控制、隐私承诺和可复现评估为优先，同时允许未来提供可选的深度复盘能力。
