# 第一版保持单动作、端侧和无账号边界

第一版只支持单个用户使用单台 iPhone，在纯侧面机位下完成保加利亚分腿蹲；用户主动选择训练腿和目标，完成个人校准后，产品识别完整动作、自动计次，并且每次动作最多给出一个证据明确的提示。核心训练不依赖账号或网络，视频默认不保存，摘要只在用户主动保存后写入本机。虽然设计中保留了账号和订阅入口，但第一版不实现账号、跨设备同步、订阅、其他动作、多人、多机位、三维分析、云端推理或医疗判断，以换取可验证的反馈质量、隐私边界和完整的本地训练闭环。
