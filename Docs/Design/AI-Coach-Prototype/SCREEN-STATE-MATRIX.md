# AI 动作教练 v3 Screen-State Matrix

本矩阵由 `ai-action-coach-prototype-v3.html` 的状态数据生成。P0 共 69 个状态，P1 共 29 个状态，总计 98 个状态。

| 页面/状态 | 所属 P0 或 P1 | 进入条件 | 用户看到的信息 | 主操作 | 次操作 | 下一状态 | 异常恢复 | 是否需要网络 | 是否上传数据 | 无障碍说明 |
|---|---|---|---|---|---|---|---|---|---|---|
| App 启动<br>`P0-LAUNCH` | P0 | 冷启动或进程重启 | **AI 动作教练**<br>正在加载端侧姿态模型；视频默认不保存。 | 继续 | 无 | `P0-SAFETY` | 加载失败进入模型失败状态 | 否 | 无 | VoiceOver 读出应用名与加载状态。 |
| 首次安全说明<br>`P0-SAFETY` | P0 | 首次安装或安全条款版本变化 | **训练前请先了解**<br>不是医疗诊断；出现疼痛或眩晕请停止；手机远离器械路径。 | 我已了解 | 查看隐私 | `P0-HOME` | 可返回后再次查看 | 否 | 无 | 先读页面标题，再读状态、事实和操作。 |
| 产品首页<br>`P0-HOME` | P0 | 已完成首次安全说明 | **开始或继续训练会话**<br>唯一职责：开始新会话、恢复草稿、查看最近会话与本机状态。 | 选择训练动作 | 恢复上次草稿 | `P0-EXERCISE-LIBRARY` | 草稿不可用时隐藏恢复入口 | 否 | 无 | 标题后依次读取主操作、草稿和最近会话。 |
| 动作库<br>`P0-EXERCISE-LIBRARY` | P0 | 首页选择开始新会话 | **这次练什么？**<br>P0 仅开放保加利亚分腿蹲；由用户明确选择，不自动猜测。 | 选择保加利亚分腿蹲 | 返回首页 | `P0-SESSION-SETUP` | 返回首页 | 否 | 无 | 动作卡具有选中名称和状态。 |
| 训练会话设置<br>`P0-SESSION-SETUP` | P0 | 已选择动作 | **设置本次会话**<br>选择训练腿与训练目标；后续各组默认沿用。 | 继续检查机位 | 更换动作 | `P0-CAMERA-GUIDE` | 返回动作库 | 否 | 无 | 分段控件使用 aria-pressed 表达选择。 |
| 侧面机位引导<br>`P0-CAMERA-GUIDE` | P0 | 会话设置完成 | **把手机放在纯侧面**<br>头、双脚和训练凳完整入镜；手机与髋部齐平并固定。 | 检查摄像头 | 为什么需要侧面 | `P0-PERMISSION-REQUEST` | 返回会话设置 | 否 | 无 | 图示附完整文字说明。 |
| 摄像头权限请求<br>`P0-PERMISSION-REQUEST` | P0 | 相机权限状态为未决定 | **只用于实时端侧识别**<br>核心训练不依赖网络；训练视频默认不保存。 | 继续并允许 | 暂不使用 | `P0-CALIBRATION-READY` | 拒绝进入权限拒绝状态 | 否 | 无 | 先读页面标题，再读状态、事实和操作。 |
| 摄像头权限拒绝<br>`P0-PERMISSION-DENIED` | P0 | 系统返回权限拒绝 | **需要摄像头权限**<br>说明原因和修复路径，不重复弹系统权限框。 | 打开系统设置 | 返回首页 | `P0-PERMISSION-RETURN-GRANTED` | 返回首页 | 否 | 无 | 错误由图标、标题和文字共同表达。 |
| 从设置返回：已授权<br>`P0-PERMISSION-RETURN-GRANTED` | P0 | 前台恢复且权限变为允许 | **摄像头权限已开启**<br>自动继续原会话的机位检查，不丢失设置。 | 继续校准 | 返回首页 | `P0-CALIBRATION-READY` | 相机不可用进入占用状态 | 否 | 无 | 先读页面标题，再读状态、事实和操作。 |
| 从设置返回：仍拒绝<br>`P0-PERMISSION-RETURN-DENIED` | P0 | 前台恢复但权限仍拒绝 | **仍未获得摄像头权限**<br>核心训练无法继续；可以再次前往设置或退出。 | 再次打开设置 | 返回首页 | `P0-PERMISSION-RETURN-GRANTED` | 返回首页 | 否 | 无 | 读出权限仍拒绝及两个恢复操作。 |
| 校准准备<br>`P0-CALIBRATION-READY` | P0 | 权限与画面质量通过 | **建立本次会话基线**<br>连续完成三次正常动作；同一会话后续组复用这条基线。 | 开始校准 | 查看校准说明 | `P0-CALIBRATION-RUNNING` | 退出可保存草稿 | 否 | 无 | 状态和三次进度均有文字。 |
| 校准进行中<br>`P0-CALIBRATION-RUNNING` | P0 | 校准已开始且画面可靠 | **正在记录会话基线**<br>只记录证据充分且完整的动作；不足时不计入三次。 | 继续完成动作 | 暂停校准 | `P0-CALIBRATION-SUCCESS` | 画面问题进入对应纠正状态 | 否 | 无 | 实时播报当前校准进度。 |
| 校准动作差异较大<br>`P0-CALIBRATION-INCONSISTENT` | P0 | 三次校准动作幅度、节奏或起始位置差异过大 | **三次动作差异较大**<br>不建立模糊基线；说明需要保持相近的正常动作再重新记录。 | 重新完成三次校准 | 退出并保存草稿 | `P0-CALIBRATION-RUNNING` | 返回校准准备或保存草稿 | 否 | 无 | 逐项读出造成不一致的动作阶段。 |
| 校准成功<br>`P0-CALIBRATION-SUCCESS` | P0 | 三次校准一致且证据充分 | **本次会话基线已建立**<br>后续多组复用，不要求每组重复校准。 | 开始第 1 组 | 重新校准 | `P0-TRAINING-ACTIVE` | 重新校准返回准备状态 | 否 | 无 | 成功同时使用图标、文字和颜色。 |
| 实时训练：正常<br>`P0-TRAINING-ACTIVE` | P0 | 训练开始且画面与推理可靠 | **第 1 组进行中**<br>完整重复一律计数；界面区分计数、稳定、问题和未计数尝试。 | 结束本组 | 暂停 | `P0-END-SET-CONFIRM` | 中断进入恢复状态 | 否 | 无 | 计数变化通过 aria-live 简短播报。 |
| 实时训练：单一提示<br>`P0-TRAINING-COACHING` | P0 | 一次计数重复存在一个或多个成立问题 | **一次只提醒一个重点**<br>该重复仍计数；其他成立问题保留到 Summary 与重复详情。 | 继续训练 | 关闭语音 | `P0-TRAINING-ACTIVE` | 证据不足则保持沉默 | 否 | 无 | 语音提示同步显示等价文字。 |
| 实时训练：无法可靠判断<br>`P0-TRAINING-UNCERTAIN` | P0 | 关键点置信度或视角证据不足 | **无法可靠判断**<br>不推断动作问题；根据完整性决定计数或记为未计数尝试。 | 调整后继续 | 结束本组 | `P0-TRAINING-ACTIVE` | 持续不足进入画面纠正 | 否 | 无 | 不只用颜色表达不确定状态。 |
| 结束本组确认<br>`P0-END-SET-CONFIRM` | P0 | 用户选择结束本组 | **结束并生成本组总结？**<br>保留全部已完成重复；未完成尝试不会被补算。 | 结束本组 | 继续训练 | `P0-SUMMARY-CLEAN` | 返回训练 | 否 | 无 | 焦点限制在确认弹层，危险与取消清晰区分。 |
| 组总结：无成立问题<br>`P0-SUMMARY-CLEAN` | P0 | 本组完成且没有成立问题 | **本组总结**<br>显示四类计数和完整问题汇总；没有成立的问题时，不会硬给建议。 | 再做一组 | 完成会话 | `P0-NEXT-SET-READY` | 保存失败进入本地保存状态 | 否 | 无 | 统计标签完整，不依赖颜色。 |
| 组总结：存在问题<br>`P0-SUMMARY-ISSUES` | P0 | 本组完成且存在成立问题 | **本组总结**<br>汇总本组发现的所有问题、出现次数和判断依据；给出最多三条下一组建议。 | 看动作详情 | 再做一组 | `P0-REP-DETAIL` | 可完成会话 | 否 | 无 | 问题和建议分区并有标题。 |
| 单次重复全部证据<br>`P0-REP-DETAIL` | P0 | 从问题聚合或组详情选择一次重复 | **该重复的全部问题**<br>同时展示该重复所有成立问题、证据来源、无法判断项和实时播报的唯一提示。 | 返回组总结 | 查看下一次重复 | `P0-SUMMARY-ISSUES` | 返回组总结 | 否 | 无 | 证据按事实、问题、未判断顺序读取。 |
| 会话组列表<br>`P0-SESSION-GROUPS` | P0 | 至少完成一组 | **本次训练会话**<br>按组展示状态；动作、训练腿、目标和校准基线属于会话级。 | 再做一组 | 完成会话 | `P0-NEXT-SET-READY` | 返回最近组总结 | 否 | 无 | 每组行的名称包含组序号和问题摘要。 |
| 下一组就绪<br>`P0-NEXT-SET-READY` | P0 | 从组总结选择再做一组 | **第 2 组准备就绪**<br>已沿用动作、训练腿、训练目标和会话校准基线。 | 开始下一组 | 修改会话设置 | `P0-TRAINING-ACTIVE` | 修改设置会标明是否需要重校准 | 否 | 无 | 明确读出已复用的四项设置。 |
| 完成训练会话<br>`P0-SESSION-COMPLETE` | P0 | 用户在组总结或组列表完成会话 | **训练会话已完成**<br>保存会话级摘要和各组结果；视频仍未保存。 | 查看会话详情 | 返回首页 | `P0-HISTORY-SESSION-DETAIL` | 保存失败进入本地保存状态 | 否 | 无 | 成功信息使用图标和文字。 |
| 人体太近<br>`P0-QUALITY-TOO-NEAR` | P0 | 身体超出合理占画比例 | **向后移动手机**<br>暂停判断与提示，不丢失会话和已完成重复。 | 重新检查 | 结束本组 | `P0-TRAINING-ACTIVE` | 持续失败可放弃本组 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 人体太远<br>`P0-QUALITY-TOO-FAR` | P0 | 关键点尺寸不足 | **把手机移近一些**<br>暂停判断，提示完整身体仍需入镜。 | 重新检查 | 结束本组 | `P0-TRAINING-ACTIVE` | 持续失败可放弃本组 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 头脚出框<br>`P0-QUALITY-OUT-OF-FRAME` | P0 | 头部或任一脚持续出框 | **让头和双脚完整入镜**<br>不使用裁切后的证据判断动作问题。 | 重新检查 | 结束本组 | `P0-TRAINING-ACTIVE` | 持续失败可放弃本组 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 身体被遮挡<br>`P0-QUALITY-OCCLUDED` | P0 | 关键关节持续被器械或物体遮挡 | **移开遮挡物**<br>指出被遮挡区域；不猜测缺失关键点。 | 重新检查 | 结束本组 | `P0-TRAINING-ACTIVE` | 持续失败可放弃本组 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 环境过暗<br>`P0-QUALITY-DARK` | P0 | 曝光不足导致关键点不稳定 | **增加正面或侧面照明**<br>暂停可靠判断，保留已完成重复。 | 重新检查 | 结束本组 | `P0-TRAINING-ACTIVE` | 持续失败可放弃本组 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 强逆光<br>`P0-QUALITY-BACKLIGHT` | P0 | 主体与背景亮度差过大 | **避免光源正对镜头**<br>建议调整机位或光源方向。 | 重新检查 | 结束本组 | `P0-TRAINING-ACTIVE` | 持续失败可放弃本组 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 不是纯侧面<br>`P0-QUALITY-NOT-SIDE` | P0 | 身体朝向偏离侧面判断范围 | **转回纯侧面**<br>实时计数和问题判断暂停，避免跨视角误判。 | 重新检查 | 结束本组 | `P0-TRAINING-ACTIVE` | 持续失败可放弃本组 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 训练腿不一致<br>`P0-QUALITY-WRONG-LEG` | P0 | 当前动作侧与会话设置不一致 | **确认正在训练右腿**<br>允许回到设置修改，但修改后明确基线是否仍适用。 | 保持右腿继续 | 修改训练腿 | `P0-TRAINING-ACTIVE` | 修改后按规则重校准 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 多人入镜<br>`P0-QUALITY-MULTI-PERSON` | P0 | 检测到多个可能主体 | **画面中只保留一名训练者**<br>暂停主体跟踪，不自动选择其中一人。 | 重新检查 | 结束本组 | `P0-TRAINING-ACTIVE` | 持续失败可放弃本组 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| App 进入后台<br>`P0-INTERRUPT-BACKGROUND` | P0 | 用户切换 App | **分析已暂停**<br>保留当前组草稿和已完成重复；后台不继续使用相机。 | 返回后继续 | 结束本组 | `P0-INTERRUPT-FOREGROUND` | 恢复时重新检查画面 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 设备锁屏<br>`P0-INTERRUPT-LOCK` | P0 | 屏幕锁定 | **训练已暂停**<br>停止相机与推理；解锁后进入前台恢复。 | 解锁后继续 | 保留草稿退出 | `P0-INTERRUPT-FOREGROUND` | 恢复草稿 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 来电中断<br>`P0-INTERRUPT-CALL` | P0 | 系统来电覆盖训练 | **训练已暂停**<br>已完成重复保留；通话结束后重新确认机位。 | 通话后继续 | 结束本组 | `P0-INTERRUPT-FOREGROUND` | 结束本组 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 音频中断<br>`P0-INTERRUPT-AUDIO` | P0 | 其他音频会话抢占或蓝牙变化 | **语音提示已暂停**<br>视觉文字反馈继续；计数与端侧判断不受影响。 | 继续静音训练 | 恢复语音 | `P0-TRAINING-ACTIVE` | 音频恢复后重新启用 | 否 | 无 | 等价文字反馈持续可见。 |
| 前台恢复检查<br>`P0-INTERRUPT-FOREGROUND` | P0 | 从后台、锁屏或来电返回 | **正在恢复训练**<br>重新检查权限、相机、主体与会话草稿，再恢复计数。 | 继续本组 | 放弃本组 | `P0-TRAINING-ACTIVE` | 失败进入对应异常状态 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 设备过热<br>`P0-DEVICE-THERMAL` | P0 | 系统热状态达到需要降载的级别 | **设备温度过高**<br>暂停相机和推理，说明冷却后可恢复；不降低精度继续运行。 | 冷却后重试 | 保存草稿退出 | `P0-INTERRUPT-FOREGROUND` | 恢复草稿 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 低电量<br>`P0-DEVICE-LOW-BATTERY` | P0 | 电量低于产品阈值 | **电量较低**<br>允许继续但提示连接电源；不影响已经成立的事实。 | 继续训练 | 保存草稿退出 | `P0-TRAINING-ACTIVE` | 退出后恢复草稿 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 相机被占用<br>`P0-CAMERA-IN-USE` | P0 | 其他应用或系统功能占用相机 | **摄像头暂时不可用**<br>不启动无画面的推理；等待资源释放。 | 重新尝试 | 返回首页 | `P0-CALIBRATION-READY` | 持续失败返回首页 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 姿态模型加载<br>`P0-MODEL-LOADING` | P0 | 端侧模型尚未就绪 | **正在准备端侧识别**<br>显示确定的加载状态；不伪造进度百分比。 | 继续等待 | 返回首页 | `P0-CALIBRATION-READY` | 失败进入模型失败 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 姿态模型加载失败<br>`P0-MODEL-LOAD-FAILED` | P0 | 模型资源校验或加载失败 | **无法加载姿态模型**<br>核心训练不可用；提供重试与本地诊断信息。 | 重新加载 | 返回首页 | `P0-MODEL-LOADING` | 返回首页 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 姿态推理失败<br>`P0-INFERENCE-FAILED` | P0 | 运行中推理连续失败 | **分析暂时停止**<br>保留会话和计数事实；重启推理前不判断新重复。 | 重新启动分析 | 结束本组 | `P0-TRAINING-ACTIVE` | 持续失败保存草稿 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 训练中退出菜单<br>`P0-EXIT-MENU` | P0 | 训练中选择关闭 | **暂停还是退出？**<br>提供继续、结束本组、放弃本组三个明确层级。 | 继续训练 | 结束本组 | `P0-TRAINING-ACTIVE` | 放弃进入确认 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 放弃本组确认<br>`P0-ABANDON-CONFIRM` | P0 | 用户选择放弃本组 | **放弃当前组？**<br>可以保留已经完成的重复，或删除整个未完成组。 | 保留完成重复 | 删除本组 | `P0-ABANDON-KEEP-REPS` | 删除后回会话组列表 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 保留已完成重复<br>`P0-ABANDON-KEEP-REPS` | P0 | 放弃本组时选择保留 | **已保留完成的重复**<br>本组标记为未完成；未完成尝试不会计数。 | 返回会话 | 查看已保留重复 | `P0-SESSION-GROUPS` | 返回会话 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 恢复训练草稿<br>`P0-DRAFT-RESUME` | P0 | 首页检测到未完成会话 | **继续上次训练？**<br>显示动作、训练腿、目标、已完成组和草稿时间；不自动恢复相机。 | 检查机位并继续 | 删除草稿 | `P0-INTERRUPT-FOREGROUND` | 删除失败保留草稿 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 保存中<br>`P0-SAVE-PROGRESS` | P0 | 组或会话完成后写入本地 | **正在保存到本机**<br>只保存结构化摘要、设置、版本与证据引用。 | 继续等待 | 无 | `P0-SAVE-SUCCESS` | 失败进入保存失败 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 保存失败<br>`P0-SAVE-FAILED` | P0 | 本地写入失败 | **暂时无法保存**<br>保留内存中的草稿，说明重试不会重复创建记录。 | 重新保存 | 稍后恢复 | `P0-SAVE-PROGRESS` | 返回首页显示草稿 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 存储空间不足<br>`P0-STORAGE-FULL` | P0 | 系统返回磁盘空间不足 | **无法保存：空间不足**<br>建议释放空间；训练视频默认未保存，不把问题归因于视频。 | 重新检查空间 | 保留草稿退出 | `P0-SAVE-PROGRESS` | 首页恢复草稿 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 保存成功<br>`P0-SAVE-SUCCESS` | P0 | 本地事务提交成功 | **已保存到本机**<br>提供查看会话和返回首页；Toast 与文字状态一致。 | 查看会话 | 返回首页 | `P0-HISTORY-SESSION-DETAIL` | 返回首页 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 删除会话确认<br>`P0-DELETE-CONFIRM` | P0 | 历史中选择删除会话 | **删除这次训练会话？**<br>删除全部组摘要且不可恢复；本版本没有保存训练视频。 | 删除会话 | 取消 | `P0-DELETE-SUCCESS` | 失败进入删除失败 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 删除失败<br>`P0-DELETE-FAILED` | P0 | 本地删除事务失败 | **暂时无法删除**<br>原记录保持完整；提供重试，不显示假成功。 | 重新删除 | 返回会话 | `P0-DELETE-CONFIRM` | 返回会话详情 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 删除成功<br>`P0-DELETE-SUCCESS` | P0 | 本地删除事务完成 | **训练会话已删除**<br>从历史移除并提供明确完成状态。 | 返回训练历史 | 返回首页 | `P0-HISTORY-SESSIONS` | 返回首页 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 训练历史空状态<br>`P0-HISTORY-EMPTY` | P0 | 没有已保存会话 | **还没有训练会话**<br>完成并保存一组训练后，会话会出现在这里。 | 开始第一次训练 | 返回首页 | `P0-EXERCISE-LIBRARY` | 返回首页 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 训练会话历史<br>`P0-HISTORY-SESSIONS` | P0 | 至少有一个已保存会话 | **训练历史**<br>按会话组织；每个会话显示动作、训练腿、组数与问题摘要。 | 查看最近会话 | 返回首页 | `P0-HISTORY-SESSION-DETAIL` | 空数据进入空状态 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 会话详情<br>`P0-HISTORY-SESSION-DETAIL` | P0 | 选择一个历史会话 | **本次训练会话**<br>展示会话级设置、校准版本和多个训练组。 | 查看第 1 组 | 删除会话 | `P0-HISTORY-GROUP-DETAIL` | 删除进入确认 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 历史组详情<br>`P0-HISTORY-GROUP-DETAIL` | P0 | 在会话详情选择一个组 | **第 1 组总结**<br>显示四类计数、问题汇总、下一组建议和每次动作详情。 | 看动作详情 | 返回会话 | `P0-REP-DETAIL` | 返回会话详情 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 我的：仅本机<br>`P0-MY-LOCAL` | P0 | 底部导航选择我的 | **我的**<br>仅提供训练历史、语音与动态效果、隐私、本地存储和版本；没有账号或订阅。 | 打开训练设置 | 查看本地隐私 | `P0-SETTINGS` | 返回首页 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 训练设置<br>`P0-SETTINGS` | P0 | 从我的进入设置 | **训练设置**<br>语音提示、等价文字、Reduce Motion 跟随系统、存储与版本信息。 | 保存设置 | 查看隐私 | `P0-MY-LOCAL` | 保存失败保持原值 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 本地隐私说明<br>`P0-PRIVACY-LOCAL` | P0 | 从首页或我的查看隐私 | **你的训练画面如何处理**<br>摄像头 → 本机关键点 → 动作判断 → 实时反馈；视频默认不保存。 | 返回我的 | 查看 P1 数据说明 | `P0-MY-LOCAL` | 返回上一页 | 否 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 首页：最大 Dynamic Type<br>`A11Y-HOME-XXXL` | P0 | 系统文字设为最大辅助字号 | **开始训练**<br>主标题、主操作和草稿入口重排为单列；不截断，不覆盖底部导航。 | 选择训练动作 | 恢复草稿 | `P0-EXERCISE-LIBRARY` | 返回标准字号由系统控制 | 否 | 无 | 最大字号下仍保持逻辑阅读顺序。 |
| 训练：最大 Dynamic Type<br>`A11Y-TRAINING-XXXL` | P0 | 训练中使用最大辅助字号 | **训练进行中**<br>计数保持远距离可读；次级状态折叠为可滚动文字区域。 | 结束本组 | 暂停 | `P0-END-SET-CONFIRM` | 返回训练 | 否 | 无 | 计数、状态、提示、操作依序读取。 |
| 组总结：最大 Dynamic Type<br>`A11Y-SUMMARY-XXXL` | P0 | 组总结使用最大辅助字号 | **本组总结**<br>四类计数、问题汇总、下一组建议与操作纵向重排；卡片自动增高且不裁切。 | 看动作详情 | 再做一组 | `P0-SUMMARY-ISSUES` | 返回标准组总结 | 否 | 无 | 先读四类计数，再读问题、判断依据与下一组建议。 |
| VoiceOver 阅读顺序<br>`A11Y-VOICEOVER-ORDER` | P0 | VoiceOver 开启 | **阅读顺序预览**<br>页面标题 → 识别状态 → 计数事实 → 当前文字提示 → 主操作 → 次操作。 | 播放顺序示例 | 返回设置 | `A11Y-VOICE-OFF-TEXT` | 返回设置 | 否 | 无 | 所有图标按钮提供具体可访问名称。 |
| Reduce Motion<br>`A11Y-REDUCE-MOTION` | P0 | 系统开启减少动态效果 | **已减少动态效果**<br>取消位移动画、倒计时缩放和骨架闪烁；保留即时状态变化。 | 继续 | 返回设置 | `P0-HOME` | 返回设置 | 否 | 无 | 状态变化通过文字与焦点管理表达。 |
| 颜色以外的状态表达<br>`A11Y-NONCOLOR-STATUS` | P0 | 系统提高对比度、颜色辨识受限或常规验收 | **状态不只依赖颜色**<br>成功、警告、错误和无法判断同时使用图标、标题、说明和可访问名称。 | 查看状态示例 | 返回设置 | `P0-SETTINGS` | 返回设置 | 否 | 无 | 图标均有等价名称，状态变化通过 aria-live 宣告。 |
| 语音关闭：等价文字<br>`A11Y-VOICE-OFF-TEXT` | P0 | 用户关闭语音提示 | **语音提示已关闭**<br>所有实时口令在屏幕固定区域显示，并保留触觉反馈选项。 | 继续静音训练 | 开启语音 | `P0-TRAINING-ACTIVE` | 返回设置 | 否 | 无 | 文字提示区域使用 aria-live polite。 |
| 高级复盘入口<br>`P1-REVIEW-ENTRY` | P1 | P0 组总结已保存 | **获取 Gemini 高级复盘**<br>训练结束后再生成，不会影响训练中的计数和提示。 | 看看能分析什么 | 继续看本组总结 | `P1-CAPABILITY-PRICE` | 返回本组总结 | 是 | 无 | 明确标注网络与云端处理。 |
| 能力说明与价格<br>`P1-CAPABILITY-PRICE` | P1 | 从高级复盘入口进入 | **高级复盘能帮你看什么**<br>说明复盘能做什么、会用哪些内容以及价格；不会修改训练记录。 | 继续购买 | 返回本组总结 | `P1-STORE-PURCHASING` | 返回本组总结 | 是 | 无 | 价格待定以文字说明，不留空白。 |
| StoreKit 购买中<br>`P1-STORE-PURCHASING` | P1 | 用户确认购买 | **正在向 App Store 确认**<br>使用系统购买流程；不收集应用内银行卡信息。 | 继续等待 | 取消 | `P1-STORE-SUCCESS` | 取消或失败进入对应状态 | 是 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 购买已取消<br>`P1-STORE-CANCELLED` | P1 | 用户取消 StoreKit sheet | **购买已取消**<br>没有扣款，也没有上传训练内容。本组总结仍然可以查看。 | 重新查看方案 | 返回本组总结 | `P1-CAPABILITY-PRICE` | 返回本组总结 | 是 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 购买失败<br>`P1-STORE-FAILED` | P1 | StoreKit 返回失败 | **暂时无法完成购买**<br>说明发生了什么，并提供重试。用户主动取消不会被当作失败。 | 再试一次 | 返回本组总结 | `P1-STORE-PURCHASING` | 返回本组总结 | 是 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 购买成功<br>`P1-STORE-SUCCESS` | P1 | StoreKit 验证通过 | **高级复盘已解锁**<br>下一步会先告诉你需要上传什么，由你确认后才开始。 | 查看上传内容 | 稍后再说 | `P1-CONSENT-DATA-LIST` | 返回本组总结 | 是 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 恢复购买<br>`P1-STORE-RESTORE` | P1 | 用户选择恢复购买 | **正在恢复购买**<br>通过 StoreKit 验证历史权益，不上传训练数据。 | 继续 | 取消 | `P1-STORE-SUCCESS` | 未找到权益返回价格页 | 是 | 无 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 上传前数据清单与同意<br>`P1-CONSENT-DATA-LIST` | P1 | 权益有效且用户发起高级复盘 | **确认要上传什么**<br>默认上传动作分析记录和少量关键动作截图；短视频保持关闭。 | 同意并继续 | 取消 | `P1-KEYFRAME-DEFAULT` | 取消不上传 | 是 | 动作分析记录 + 关键动作截图 | 逐项读出数据类型、目的和开关状态。 |
| 默认上传方案<br>`P1-KEYFRAME-DEFAULT` | P1 | 用户同意默认数据范围 | **只上传复盘需要的内容**<br>动作分析记录和关键动作截图已选择，短视频保持关闭。 | 开始准备 | 调整上传内容 | `P1-PREPARE` | 返回上传清单 | 是 | 动作分析记录 + 关键动作截图 | 开关同时读出用途与默认状态。 |
| 短视频独立 opt-in<br>`P1-VIDEO-OPT-IN` | P1 | 用户主动打开短视频上传 | **是否额外发送短视频？**<br>默认关闭；单独说明用途、大小、保存时间和立即删除能力。 | 明确同意发送 | 保持关闭 | `P1-PREPARE` | 返回默认方案 | 是 | 动作分析记录 + 关键动作截图 + 短视频 | 同意控件不可预选。 |
| 准备高级复盘数据<br>`P1-PREPARE` | P1 | 已完成数据同意 | **正在准备复盘内容**<br>正在这台 iPhone 上整理动作分析记录和关键动作截图。 | 继续等待 | 取消 | `P1-UPLOADING` | 取消并删除临时内容 | 是 | 尚未上传 | 进度不伪造百分比。 |
| 上传中<br>`P1-UPLOADING` | P1 | 本地数据包准备完成且网络可用 | **正在安全上传**<br>显示已上传与剩余数据类型；允许转入后台。 | 在后台继续 | 取消并删除 | `P1-QUEUED` | 取消后请求删除已上传部分 | 是 | 用户同意的数据 | 进度条有数值文本和可访问名称。 |
| 等待分析<br>`P1-QUEUED` | P1 | 上传完成且任务已创建 | **高级复盘正在排队**<br>你可以先看训练结果，排队不会影响其他训练。 | 看训练结果 | 在后台继续 | `P1-ANALYZING` | 网络恢复后刷新状态 | 是 | 已上传 | 状态通过文字与系统通知说明。 |
| Gemini 分析中<br>`P1-ANALYZING` | P1 | 云端任务进入分析 | **正在生成高级复盘**<br>Gemini 只会解读训练结束后的内容，不会修改训练计数和问题记录。 | 在后台继续 | 取消并删除 | `P1-BACKGROUND-CONTINUE` | 失败进入可恢复异常 | 是 | 已上传 | 旋转动画在 Reduce Motion 下静止。 |
| 后台继续<br>`P1-BACKGROUND-CONTINUE` | P1 | 用户离开任务页 | **高级复盘会在后台继续**<br>完成后会通知你，不需要停在这个页面等待。 | 返回首页 | 看训练结果 | `P1-COMPLETE` | 通知关闭时在首页显示完成状态 | 是 | 已上传 | 不要求用户停留等待。 |
| 高级复盘完成<br>`P1-COMPLETE` | P1 | 异步任务成功完成 | **高级复盘已完成**<br>训练记录、Gemini 的解读、看不准的地方和下一组建议已经分开整理。 | 查看复盘 | 稍后查看 | `P1-RESULT-LAYERED` | 返回首页 | 是 | 已上传 | 完成状态同时使用文字和图标。 |
| 无网络<br>`P1-NO-NETWORK` | P1 | 准备或上传阶段没有网络 | **等待网络连接**<br>本组总结仍然可以查看，准备好的内容还留在这台 iPhone 上。 | 网络恢复后重试 | 看训练结果 | `P1-PREPARE` | 回到本组总结 | 是 | 未上传 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 分析超时<br>`P1-TIMEOUT` | P1 | 任务超过产品阈值 | **高级复盘用时较长**<br>这次需要多一点时间。你可以继续等待，或取消并删除已上传内容。 | 继续等待 | 取消并删除 | `P1-ANALYZING` | 返回训练结果 | 是 | 已上传 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 服务不可用<br>`P1-SERVICE-UNAVAILABLE` | P1 | 服务返回明确不可用状态 | **高级复盘暂时不可用**<br>训练结果不受影响。你可以稍后再试，也可以删除已经上传的内容。 | 稍后重试 | 删除上传内容 | `P1-RETRY` | 进入上传内容删除 | 是 | 已上传 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 速率限制<br>`P1-RATE-LIMITED` | P1 | 服务返回速率限制 | **尝试得有点频繁**<br>请稍后再试；可以再次尝试时，我们会在这里告诉你。 | 稍后重试 | 看训练结果 | `P1-RETRY` | 返回本组总结 | 是 | 已上传 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 额度用尽<br>`P1-QUOTA-EXHAUSTED` | P1 | 权益额度不足 | **本期高级复盘次数已用完**<br>新的可用次数恢复后，你可以再次使用高级复盘。 | 查看可用次数 | 看训练结果 | `P1-CAPABILITY-PRICE` | 返回本组总结 | 是 | 未新增上传 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 重试高级复盘<br>`P1-RETRY` | P1 | 网络或服务异常后用户重试 | **准备再试一次**<br>继续使用你刚才同意上传的内容；如果同意已经失效，会再次请你确认。 | 再试一次 | 取消并删除 | `P1-PREPARE` | 返回训练结果 | 是 | 按原同意范围 | 先读页面标题，再读状态、事实和操作；状态不只依赖颜色。 |
| 高级复盘分层结果<br>`P1-RESULT-LAYERED` | P1 | 高级复盘完成且无事实冲突 | **这组动作复盘**<br>训练记录、Gemini 的解读、还不能确定的地方和下一组建议分开呈现。 | 看动作详情 | 管理已上传内容 | `P0-REP-DETAIL` | 进入已上传内容管理 | 是 | 已上传 | 四个区域均有明确标题。 |
| Gemini 与端侧冲突<br>`P1-RESULT-CONFLICT` | P1 | AI 解释与端侧客观事实冲突 | **有一处需要确认**<br>训练记录不会被修改；对不上的内容也不会变成下一组建议。 | 看看哪里不一致 | 回到训练结果 | `P0-REP-DETAIL` | 返回动作复盘 | 是 | 已上传 | 冲突由文字、图标和 Amber 共同表达。 |
| 高级复盘不确定内容<br>`P1-RESULT-UNCERTAIN` | P1 | AI 对部分证据无法形成可靠解释 | **这几处还看不准**<br>说明缺少哪些画面；看不准的部分不会被写成结论或建议。 | 看训练结果 | 管理已上传内容 | `P1-RESULT-LAYERED` | 进入已上传内容管理 | 是 | 已上传 | 不确定内容使用完整句子。 |
| 云端数据保留<br>`P1-CLOUD-RETENTION` | P1 | 用户查看高级复盘数据管理 | **管理已上传内容**<br>列出动作分析记录、关键动作截图和可选视频的保存状态。 | 删除已上传内容 | 回到复盘 | `P1-CLOUD-DELETING` | 返回动作复盘 | 是 | 已上传 | 每类数据都有保留与删除状态。 |
| 云端立即删除中<br>`P1-CLOUD-DELETING` | P1 | 用户确认立即删除 | **正在删除上传内容**<br>本机训练记录会继续保留；删除会在后台完成。 | 继续等待 | 回到复盘 | `P1-CLOUD-DELETE-SUCCESS` | 失败进入删除失败 | 是 | 删除中 | 进度状态通过 role=status。 |
| 云端删除成功<br>`P1-CLOUD-DELETE-SUCCESS` | P1 | 服务确认删除完成 | **已删除上传内容**<br>逐项显示动作分析记录和关键动作截图已删除；未上传的视频保持未上传。 | 回到复盘 | 返回首页 | `P1-RESULT-LAYERED` | 返回首页 | 是 | 已删除 | 成功不只使用绿色。 |
| 云端删除失败<br>`P1-CLOUD-DELETE-FAILED` | P1 | 删除请求失败或未确认 | **还不能确认已删除**<br>保留删除请求并允许重试；没有确认前不会显示删除成功。 | 再试一次 | 返回上传内容 | `P1-CLOUD-DELETING` | 返回已上传内容管理 | 是 | 等待确认 | 错误说明包含下一步。 |
